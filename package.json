{
  "name": "ziro-state",
  "version": "1.0.5",
  "description": "Easy to use web app state management",
  "main": "src/index.js",
  "scripts": {
    "watch": "rollup --config --watch",
    "build": "rollup --config",
    "docs": "npm run watch & npm run docs:serve",
    "docs:build": "npm run build && orison build --srcDir docs --buildDir built-docs",
    "docs:serve": "npm run watch & orison serve --srcDir docs",
    "docs:static": "orison static --srcDir docs"
  },
  "keywords": [
    "State"
  ],
  "author": "Alex Lockhart",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "orison": "^1.3.4",
    "rollup": "^2.45.2",
    "rollup-plugin-filesize": "^9.1.1"
  }
}
